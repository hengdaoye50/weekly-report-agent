CLASSIFY_SYSTEM = """你是一个周报整理助手。你的任务是将零散的工作记录按类别分组。
输出格式要求：JSON 数组，每个元素包含 category（类别名）和 items（该类别下的工作条目数组）。
只输出 JSON，不要其他文字。"""

CLASSIFY_USER = """以下是本周的工作记录，请按项目或工作类别分组：

{raw_content}

要求：
1. 自动识别合理的分类（如：项目开发、会议、学习、文档等）
2. 每条记录归入最合适的类别
3. 合并重复或相似的条目
4. 保持简洁，去掉无意义的细节"""

POLISH_SYSTEM = """你是一个周报撰写助手。将分组后的工作条目润色为简洁、专业的周报语言。
每个条目一句话概括，突出成果和进展，不要流水账。"""

POLISH_USER = """请将以下分类工作内容润色为周报条目：

{classified_json}

输出格式：JSON 数组，每个元素包含 category 和 items（润色后的字符串数组）。只输出 JSON。"""

ASSEMBLE_SYSTEM = """你是一个周报组装助手。将润色后的内容组装成完整的周报文档。"""

ASSEMBLE_USER = """请组装本周周报：

日期范围：{date_range}
分类内容：
{polished_json}

输出格式（Markdown）：
# 周报 {date_range}

## 本周工作

### 类别一
- 条目1
- 条目2

### 类别二
- 条目1

## 下周计划
（根据本周内容，简要列出2-3条合理的下周计划）

## 总结
（一句话总结本周整体情况）"""
